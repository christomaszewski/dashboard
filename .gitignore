# Dependencies & build artifacts
node_modules/
app/dist/
*.tsbuildinfo

# Local Claude Code settings (personal, not shared)
.claude/settings.local.json

# OS / editor
.DS_Store
*.local

# Claude Code dev-server launch config (machine-specific vehicle IP)
.claude/launch.json

# python (agent/)
__pycache__/
*.pyc

# launcher-rendered files (dash-up: the rate-limited sidecar config)
var/
